#include <stdio.h>
#include "main.h"

/**
 * print_rev - function that prints a string
 * in reverse, followed by a new line
 * @s: parameter for string
 * Return: 0
 */

void print_rev(char *s)
{
	char s[16];

	_putchar("Reverse of the string: %s\n", s);
	_putchar('\n');
}
